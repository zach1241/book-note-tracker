<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Book</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <h1 class="header">Edit Book</h1>
  <form action="/edit/<%= book.id %>" method="POST">
    <label>Title:</label>
    <input type="text" name="title" value="<%= book.title %>" required>

    <label>Author:</label>
    <input type="text" name="author" value="<%= book.author %>" required>

    <label>Rating:</label>
    <input type="number" name="rating" min="1" max="10" value="<%= book.rating %>" required>

    <label>Review:</label>
    <textarea name="review" rows="4" required><%= book.review %></textarea>

    <button type="submit">Save Changes</button>
  </form>
  <a href="/">← Back to Home</a>
    <footer>
    <p>Made by Zachary Clint Swanepoel 💻 | Powered by Node.js + PostgreSQL</p>
  </footer>
</body>
</html>
